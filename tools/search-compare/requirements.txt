# Search-compare notebook — runtime dependencies.
# Install with uv (recommended):
#   uv venv && uv pip install -r requirements.txt

anthropic>=0.40
requests>=2.31
pandas>=2.2
python-dotenv>=1.0
ipywidgets>=8.1
jupyterlab>=4.2

# Dev / tests only:
pytest>=8.0
