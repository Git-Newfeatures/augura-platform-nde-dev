# Frontend apps/web — real mode only (no mock layer).
# Supabase Auth (login + JWT sent to the backend)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# FastAPI backend (Modal domain in prod, http://localhost:8000 in dev)
VITE_API_URL=https://your-modal-app.modal.run
