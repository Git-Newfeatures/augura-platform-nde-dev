# Frontend apps/web — mode réel uniquement (pas de couche mock).
# Supabase Auth (login + JWT envoyé au backend)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Backend FastAPI (domaine Modal en prod, http://localhost:8000 en dev)
VITE_API_URL=https://your-modal-app.modal.run
